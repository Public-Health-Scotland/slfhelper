# Check if the current user is in a UNIX group

Check if the current user is in a UNIX group

## Usage

``` r
check_has_access(group = "hscdiip")
```

## Arguments

- group:

  The group to check, default is hscdiip

## Value

Boolean
